/**
 * @description Basic test queuable that can be fed in to test certain functions
 * @author Tom Wilkins
 * @group Core
 */
@IsTest
public without sharing class CoreLibs_TestQueueable implements Queueable {
  public void execute(QueueableContext ctx) {
  }
}
